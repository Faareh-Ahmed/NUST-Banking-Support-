# Documentation

This directory contains project documentation assets that were previously scattered at repository root.

## Contents

- `architecture.md` — detailed system architecture diagram, data flow, and component responsibilities.
- `images/` — screenshots used by the root `README.md`.

## Why this folder exists

Keeping docs under `docs/` makes the repository root cleaner and easier to navigate, while preserving all submission artifacts in one place.
