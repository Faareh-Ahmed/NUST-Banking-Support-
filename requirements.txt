# ── LLM (Local inference — Qwen3-1.7B on CPU, no GPU required) ──────────────
torch>=2.0.0                       # PyTorch CPU build — bfloat16 inference on Intel Core Ultra
transformers>=4.51.0               # Qwen3 support requires 4.51+
accelerate>=0.30.0                 # low_cpu_mem_usage shard-by-shard weight loading
huggingface_hub>=0.23.0            # used internally by transformers + sentence-transformers

# ── Local Embeddings ──────────────────────────────────────────────────────────
sentence-transformers>=2.2.2       # all-MiniLM-L6-v2 (22M params, CPU-fast, ~100 MB)

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb>=1.0.0
protobuf>=3.20.0,<4.26.0           # chromadb 1.x requires protobuf <4.26

# ── Data Processing ───────────────────────────────────────────────────────────
openpyxl>=3.1.2                    # Excel knowledge-base parsing
pandas>=2.0.0

# ── Backend Framework ─────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9            # file upload handling

# ── Utilities ─────────────────────────────────────────────────────────────────
regex>=2023.0
